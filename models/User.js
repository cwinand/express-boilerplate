"use strict";

var uuid = require('node-uuid');

class User {

    static findAll () {
    }

    static findByID (id) {
    }

    static create (attributes) {
    }

    static save (attributes) {
    }

    static destroy (id) {
    }
}

module.exports = User;